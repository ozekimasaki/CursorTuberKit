{
  "nodeModulesDir": "auto",
  "tasks": {
    "typecheck": "deno run -A npm:typescript/bin/tsc -p tsconfig.json --noEmit && deno run -A npm:typescript/bin/tsc -p tsconfig.node.json --noEmit",
    "build": "deno task typecheck && deno run -A npm:vite build && deno run -A npm:typescript/bin/tsc -p tsconfig.node.json",
    "start": "NODE_ENV=production deno run --allow-env --allow-net --allow-read dist/server/index.js"
  }
}
